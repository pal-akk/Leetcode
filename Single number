int singleNumber(vector<int>& nums) {
        int n=nums.size();
        if(n==1) return nums[0];
        else{
            int result=0;
            for(int i=0;i<n;i++){
                result^=nums[i];
            }
            return result;
        }
         
    }
