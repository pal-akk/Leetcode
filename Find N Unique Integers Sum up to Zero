vector<int> sums;
        int a=1;
        int i=0;
        while(i<(n/2)){
            sums.push_back(a);
            sums.push_back(-a);
            a+=1;
            i+=1;
        }
        if(n%2!=0){
            sums.push_back(0);
        }
        return sums;
